---
/* Astro stuff goes here */
---
<style>
.tag-line {
  padding-block-start: clamp(21px, 5vw, 64px);
  padding-block-end: clamp(42px, 12vw, 126px);
  display: grid;
  grid-template-columns: 7fr 1fr;
}

.tag-line h2 {
  font-size: clamp(2rem, 6vw + 1rem, 4.5rem);
  font-family: var(--font-display);
  font-weight: 800;
  line-height: 1.2;
}
</style>

<div class="tag-line">
  <h2>
    <slot />
  </h2>
</div>
